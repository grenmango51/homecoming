# Runtime dependencies. Python 3.11+ is required (the config loader uses tomllib).
#
# Install with:
#   python -m pip install -r requirements.txt
#   python -m patchright install chromium     # or: python -m playwright install chromium

# Browser automation for both scrapers.
playwright>=1.40

# Patched Playwright build used by the Skyscanner scraper to avoid CDP
# automation leaks. Optional: src/skyscanner.py falls back to plain Playwright,
# but Skyscanner's bot detection is far more likely to challenge you without it.
patchright>=1.40

# Point-of-sale arbitrage analyzers and report tables.
pandas>=2.0
tabulate>=0.9
